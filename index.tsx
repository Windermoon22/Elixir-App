import { registerRootComponent } from 'expo';
import App from './App';

registerRootComponent(App);

export default App;
// This ensures the app is set up correctly regardless of whether you
// load it in Expo Go or in a native build.